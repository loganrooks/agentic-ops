# Phase P1.5 — Docs restructure + review-discipline hardening

**Goal:** Course-correct the review discipline before security-sensitive phases (P2/P3/P4) land. Restructure the monolithic plan into reviewable per-phase docs, install CodeRabbit, apply branch protection on main, and update operative-discipline docs (CONTRIBUTING.md, AGENTS.md) to require CodeRabbit review before merge.

**Status:** in_progress (PR not yet open)

**Branch:** `chore/p1.5-docs-restructure`

**PR:** (TBD; opened in P1.5-T12)

## Why this phase exists

P1 (PR #1) was squash-merged on CI green via `gh pr merge --auto` without CodeRabbit review. The original plan specified `--auto`, but did not account for CodeRabbit. The user identified this immediately as a process violation. P1.5 is the course-correction.

This phase is NOT in the original plan at `.planning/auto-execution/PLAN-snapshot.md`. It is an explicit course-correction approved by the user (option B: leave PR #1 merged, ship docs restructure + discipline hardening as P1.5).

## Phase entry preconditions
- CHECKPOINT-P1 exists
- CodeRabbit GitHub App installed on agentic-ops (HUMAN-GATE-1.5; complete)
- Branch protection on main applied via gh CLI (complete)
- v1 tag at 3f5d05a (post-P1 merge)

## Phase exit postconditions
- All cross-cutting docs in `.planning/` exist (INITIATIVE, PHASE-MAP, EXECUTION-MODEL, GUARDRAILS, HUMAN-GATES, RISK-REGISTER)
- Per-phase docs `.planning/phases/PN-*.md` exist for P0..P9 + EMPIRICAL-GATE
- `.coderabbit.yaml` present and inheriting from loganrooks/coderabbit central config
- CONTRIBUTING.md updated with explicit CodeRabbit-required-before-merge section
- AGENTS.md hard rule "do not merge without CodeRabbit review" present
- CI green
- CodeRabbit reviewed the PR and conversations resolved
- PR merged
- v1 tag force-updated to merge SHA

## Tasks

### P1.5-T1 — Add .coderabbit.yaml
- **Action:** Mirror CBM's pattern (`inheritance: true`) and add agentic-ops-specific path_instructions for review.yml, post-claude-review.sh, test-dispatcher.sh, ADRs, .planning/phases/, .planning/auto-execution/, ci.yml, AGENTS.md, SECURITY.md.
- **Postcondition:** File exists; valid YAML; passes yamllint.

### P1.5-T2 — Update CONTRIBUTING.md
- **Action:** Add explicit "Wait for CI green AND CodeRabbit review" step in the contribution workflow. Add a "Review discipline" section explaining the CodeRabbit + maintainer flow. Forbid `--auto` immediately on PR open.
- **Postcondition:** Section "Review discipline" present; "CodeRabbit" mentioned explicitly.

### P1.5-T3 — Update AGENTS.md hard rules
- **Action:** Add hard rule "Do not merge without CodeRabbit review" as the first hard rule. Add hard rule "Do not force-push branches" (v1 tag is the only exception).
- **Postcondition:** Both hard rules present in AGENTS.md.

### P1.5-T4..T9 — Cross-cutting docs
- **Action:** Write `.planning/INITIATIVE.md`, `PHASE-MAP.md`, `EXECUTION-MODEL.md`, `GUARDRAILS.md`, `HUMAN-GATES.md`, `RISK-REGISTER.md`. Content extracted from PLAN-snapshot.md sections.
- **Postcondition:** All 6 files exist with substantive content.

### P1.5-T10 — Per-phase docs
- **Action:** Write `.planning/phases/PN-*.md` for P0..P9 plus EMPIRICAL-GATE. P0/P1/P1.5 are retrospective; P2..P9 are forward-looking. Use parallel agents to write all 12 files concurrently.
- **Postcondition:** All 12 phase docs exist.

### P1.5-T11 — Local validation
- **Action:** Run yamllint on .coderabbit.yaml, run actionlint on workflows (no changes; should still pass), verify markdown structural integrity (no broken cross-references, all referenced phase docs exist).
- **Postcondition:** All linters clean.

### P1.5-T12 — Open PR
- **Action:** Stage all `.planning/` files, `.coderabbit.yaml`, CONTRIBUTING.md, AGENTS.md updates. Do NOT include `.planning/auto-execution/` state files (those are uncommitted during execution; committed only in P9 archive). Open PR with detailed body explaining the course-correction.
- **Postcondition:** PR open; CI running.

### P1.5-T13 — Wait for CI + CodeRabbit
- **Action:** Wait for CI green. Wait for CodeRabbit to review (within ~10 min of PR open; if longer, comment `@coderabbitai review`). Address findings via fixup commits. Resolve all conversations.
- **Postcondition:** CI green; CodeRabbit posted at least one review; all conversations resolved.

### P1.5-T14 — Merge after maintainer signal
- **Action:** Wait for explicit user signal to merge. Merge via squash. Force-update v1 tag.
- **Postcondition:** PR merged; v1 tag at new SHA.

### P1.5-T15 — Phase checkpoint
- **Action:** Write CHECKPOINT-P1.5.md, append to CHECKPOINTS.md, advance STATE.md to P2.
- **Postcondition:** Checkpoint file exists.

## Phase total estimate
~2 hrs, 15 tasks, ~1 session.

## Notes
- This phase is explicitly NOT in the original plan. The decimal numbering signals insertion without renumbering. Future phases (P2..P9) retain their original numbering and content.
- All P1.5 work follows the new review discipline: CodeRabbit must review before merge, conversation resolution is required (enforced by branch protection).
- The PR #1 merge was a one-time process violation; subsequent merges (including P1.5 itself) follow the corrected discipline.
